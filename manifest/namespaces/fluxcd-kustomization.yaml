apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: namespaces
  namespace: flux-system
spec:
  interval: 10s

  sourceRef:
    kind: OCIRepository
    name: flux-system

  path: namespaces

  prune: true
  wait: true
  deletionPolicy: WaitForTermination
