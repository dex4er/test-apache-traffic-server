# Single storage area for the L2 disk cache.
# All origins (podinfo-0, podinfo-1) share this volume — no per-host isolation.
# WARNING: changing the size value wipes the disk cache on restart.

/var/cache/ats 256M
