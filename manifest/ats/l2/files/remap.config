# L2 receives absolute-URL requests forwarded by L1 after L1 has already
# applied its own remap rules. remap_required=0 (records.yaml) allows ATS to
# forward those requests to origin without matching them against remap rules here.
# No remap rules are therefore needed for L2.
